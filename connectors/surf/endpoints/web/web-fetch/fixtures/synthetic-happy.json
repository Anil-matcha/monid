{
    "name": "synthetic-happy",
    "description": "Synthetic (no SURF key held at port time, 2026-09-16): the `{ data, meta }` envelope and the `{ error: { code, message } }` error shape follow the v1 adaptor tests and the v1 design's live-verified upstream contract; field values are placeholders. Unverified against live traffic — replace via `deno task record`. A 2xx with rows in `data` — the flat 1-credit line settles once; `meta.credits_used` stays in the body (design D1).",
    "calls": [
        {
            "req": {
                "method": "GET",
                "url": "https://api.asksurf.ai/gateway/v1/web/fetch?url=https%3A%2F%2Fethereum.org&timeout=30000"
            },
            "res": {
                "status": 200,
                "body": {
                    "data": {
                        "id": "00000000-0000-4000-8000-000000000001",
                        "name": "Example",
                        "value": 1
                    },
                    "meta": {
                        "credits_used": 1,
                        "cached": false
                    }
                }
            }
        }
    ]
}
